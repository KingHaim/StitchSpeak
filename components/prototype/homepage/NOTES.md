# Homepage prototype — verdict placeholder

**Question:** What should the StitchSpeak homepage / first-run experience feel like?

**Variants**

| Key | Name | Structure |
|-----|------|-----------|
| A | Editorial landing | Marketing hero → features → pattern demo (closest to current `LandingPage`) |
| B | Tool-first workshop | Upload workspace is the hero; marketing lives in a slim side rail |
| C | App shell preview | Sidebar nav + studio preview; guest mode banner instead of long scroll |

**How to view**

```bash
npm run dev
```

Open: `http://localhost:5173/?prototype=homepage&variant=A` (or `B`, `C`)

Use the bottom switcher or ← / → keys.

**Verdict:** _TBD — pick a winner or mix (e.g. header from C + hero from B) before deleting this folder._
